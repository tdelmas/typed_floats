
jobs:
  build:
    docker:
      - image: cimg/rust:1.70.0
    steps:
      - checkout
      - run: cargo build
  test:
    docker:
      - image: cimg/rust:1.70.0
    steps:
      - checkout
      - run: cargo test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build